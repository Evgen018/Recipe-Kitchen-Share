# План добавления черногорского языка (латиница) и переключателя RU / ME

## Цели

- Добавить второй язык: **черногорский (латиница)** — код `cnr`.
- Переключатель языков в виде кнопок-флагов: 🇷🇺 (русский), 🇲🇪 (черногорский).
- **Не менять** маршруты, API, поиск, пагинацию, логику приложения.
- Хранить выбранный язык в **cookie** (`NEXT_LOCALE`), без префиксов в URL.

---

## Что переводим

- Все **UI-строки**: кнопки, заголовки, подписи, плейсхолдеры, сообщения об ошибках, подсказки.
- Формы (новый рецепт, правка, категории), сайдбар, хедер, футер, таблица/карточки, пагинация, сортировка, вид.

## Что не переводим

- **Данные пользователей**: названия и текст рецептов, названия категорий в БД.
- **URL**: `/`, `/dashboard`, `/dashboard/public`, `?q=`, `?page=`, `?view=`, `?sort=` — без изменений.
- Название продукта **Recipe-Kitchen-Share** можно оставить как есть (по желанию добавить перевод слогана на главной).

---

## Шаг 1. Инфраструктура i18n

**Файлы:**

- `lib/i18n.ts`:
  - `getLocale()` — читает `cookies().get('NEXT_LOCALE')` → `'ru' | 'cnr'`, по умолчанию `'ru'`. Обернуть в `cache()` из React для одного запроса.
  - `getT(locale)` — возвращает `(key: string) => messages[locale][key] ?? key`.
  - Типы: `Locale = 'ru' | 'cnr'`.
- `messages/ru.json` — плоский объект ключей и русских строк.
- `messages/cnr.json` — те же ключи, черногорские строки (латиница).

**Ключи** — короткие, в стиле `section.element` или `common.button`, например:

- `header.logout`, `header.login`
- `home.heroTitle`, `home.heroSubtitle`, `home.loginGoogle`
- `home.recentRecipes`, `home.popularRecipes`, `home.noRecipes`, `home.noPopular`
- `sidebar.profile`, `sidebar.categories`, `sidebar.myRecipes`, `sidebar.public`, `sidebar.favorites`, `sidebar.history`, `sidebar.settings`, `sidebar.menu`, `sidebar.openMenu`, `sidebar.closeMenu`, `sidebar.user`
- `dashboard.title`, `dashboard.myRecipes`, `dashboard.public`, `dashboard.newRecipe`, `dashboard.noRecipes`, `dashboard.searchPlaceholder`
- `pagination.prev`, `pagination.next`, `pagination.of`
- `view.cards`, `view.table`, `view.cardsTitle`, `view.tableTitle`
- `sort.popular`, `sort.recent`, `sort.popularTitle`, `sort.recentTitle`, `sort.label`, `view.label`
- `recipe.title`, `recipe.content`, `recipe.category`, `recipe.public`, `recipe.private`, `recipe.edit`, `recipe.new`, `recipe.delete`, `recipe.download`, `recipe.fav`, `recipe.unfav`, `recipe.like`, `recipe.unlike`, `recipe.updated`, `recipe.categoryLabel`, `recipe.status`, `recipe.chooseCategory`, `recipe.titlePlaceholder`
- `table.title`, `table.description`, `table.category`, `table.updated`, `table.status`, `table.likes`, `table.actions`
- `categories.title`, `categories.add`, `categories.of`, `categories.empty`, `categories.recipe`, `categories.recipes2`, `categories.recipes5`
- `footer.contacts`, `footer.about`, `footer.devHint`
- `errors.authRequired`, `errors.notFound`, `errors.noPermission`, `errors.tryLater`, `errors.validation`, `validation.titleRequired`, `validation.contentRequired`, `validation.categoryRequired`, `validation.nameRequired`, `validation.nameTooLong`, `validation.categoryExists`, `validation.categoryLimit`
- и т.д. по мере инвентаризации строк в коде.

**Важно:**  
Сначала сделать минимальный набор ключей (хедер, футер, главная, дашборд, сайдбар), убедиться, что переключение работает. Затем постепенно добавлять ключи для остальных экранов.

---

## Шаг 2. Переключатель языков (флаги)

**Файлы:**

- `app/components/LangSwitcher.tsx` (Client Component):
  - Две кнопки: 🇷🇺 и 🇲🇪.
  - При клике: записать в cookie `NEXT_LOCALE` (`ru` или `cnr`), вызвать `router.refresh()`.
  - Визуально выделять активный язык (например, бордер/фон).
  - Использовать `aria-label` / `title` для доступности («Русский», «Crnogorski»).

**Размещение:**  
В `Header`, рядом с кнопками «Вход» / «Выход» и именем пользователя. Имеет смысл всегда показывать переключатель (и для авторизованных, и для гостей).

**Cookie:**  
- Имя: `NEXT_LOCALE`  
- Значения: `ru` | `cnr`  
- Устанавливать через `document.cookie` или небольшой route handler `POST /api/locale` с `Set-Cookie` + редирект/refresh — как удобнее. При использовании только cookie без API достаточно `js-cookie` или собственной утилиты на клиенте.

---

## Шаг 3. Провайдер локали и контекст (клиент)

**Файлы:**

- `app/components/LocaleProvider.tsx` (Client Component):
  - Принимает проп `locale: 'ru' | 'cnr'`.
  - Предоставляет контекст: `{ locale, t }`, где `t(key)` строится из `messages[locale]`.
  - Дочерние Client Components используют `useLocale()` / `useTranslations()` и получают `locale` и `t`.

**Размещение:**  
В `app/layout.tsx`: корневой layout читает `getLocale()` из cookie, рендерит  
`<LocaleProvider locale={locale}>` вокруг `Header`, `main`, `Footer`.  
Таким образом, все клиентские компоненты (сайдбар, диалоги, карточки, таблица, переключатель вида/сортировки и т.д.) получают доступ к `locale` и `t` без смены маршрутов.

---

## Шаг 4. Интеграция в Layout и глобальные компоненты

**`app/layout.tsx`:**

- Вызвать `getLocale()`.
- Установить `<html lang={locale === 'cnr' ? 'cnr' : 'ru'}>` (при необходимости поправить под допустимые значения `lang`).
- Обернуть всё в `<LocaleProvider locale={locale}>`.
- Передавать в `Header` и `Footer` пропы `locale` и `t` (или только `locale`, если они сами импортируют `getT` и `messages` — но тогда им нужен доступ к `getLocale` на сервере). Проще передать `locale` и `t` из layout.

**`Header`:**

- Серверный компонент. Принимает `locale`, `t`.
- Заменить все жёстко заданные строки на `t('...')` (например, «Вход», «Выход»).
- Вставить `<LangSwitcher />` в навбар. `LangSwitcher` может получать `locale` пропом, чтобы подсветить активный флаг.

**`Footer`:**

- Аналогично: `locale`, `t`. Строки «Контакты», «О нас» и т.п. через `t(...)`.
- `FooterPlaceholderLinks`: при показе «Страница в разработке» использовать `t('footer.devHint')`. Компонент клиентский — брать `t` из `useTranslations()`.

---

## Шаг 5. Dashboard и страницы (Server Components)

**Общие правила:**

- На каждой странице (или в `dashboard/layout`) вызывать `getLocale()` и `getT(getLocale())`.
- Все заголовки, подзаголовки, пустые состояния, пагинация («Назад», «Вперёд», «N из M») перевести на `t(...)`.

**Конкретно:**

- `app/page.tsx` (главная): hero, секции «Недавние» / «Популярные», кнопка «Войти через Google», пустые состояния.
- `app/dashboard/page.tsx`: «Личный кабинет», «Мои рецепты», «Новый рецепт», «У вас пока нет рецептов…», пагинация.
- `app/dashboard/public/page.tsx`: «Личный кабинет», «Публичные рецепты», «Пока нет публичных рецептов», пагинация.
- `app/dashboard/categories/page.tsx`: заголовки, «X из 20 категорий», «Добавить категорию», пустое состояние, склонения «рецепт»/«рецепта»/«рецептов».
- `app/dashboard/favorites/page.tsx`, `history`, `profile`, `settings` — по тому же принципу.

**Важно:**  
Не менять запросы к БД, логику пагинации, `searchParams` (`q`, `page`, `view`, `sort`). Только замена отображаемого текста на `t(...)`.

---

## Шаг 6. Клиентские компоненты дашборда

Компоненты переходят на `useTranslations()` (и при необходимости `useLocale()`), все строки — через `t(...)`:

- `DashboardSidebar`: пункты меню, «Меню», «Открыть/Закрыть меню», «Пользователь».
- `SearchRecipes`: плейсхолдер поиска (например, `dashboard.searchPlaceholder`).
- `ViewToggle`: «Карточки», «Таблица», `title`/`aria-label`.
- `SortSwitcher`: «Популярные», «Новые», подсказки.
- `RecipeDialog`: «Новый рецепт» / «Редактировать рецепт», поля, кнопки, «Выберите категорию».
- `RecipeViewModal`: «Правка», «Редактировать рецепт», «Закрыть», форма, кнопки.
- `RecipeCard` / `RecipeTable`: кнопки (правка, удаление, скачать, лайк, избранное), статусы «Публичный»/«Приватный», тултип «Категория», `confirm('Удалить рецепт?')`, заголовки колонок.
- `LikeButton`: «Нравится», «Убрать «Нравится»», сообщение об ошибке (см. ниже).
- `AddCategoryDialog`: «Добавить категорию», «Название», плейсхолдер, «Отмена», «Добавить».
- `FooterPlaceholderLinks`: «Контакты», «О нас», «Страница в разработке».

При экспорте в файл (рецепт) заголовки «Категория», «Обновлено», «Статус» и значения «Публичный»/«Приватный» можно тоже брать из `t(...)` в соответствии с текущей локалью в момент скачивания.

---

## Шаг 7. Server Actions и валидация

**Стратегия:**  
Вместо готовых фраз возвращаем **ключи** ошибок. Отображение — на клиенте через `t(key)`.

**`app/actions/recipes.ts`:**

- Zod: заменить сообщения на ключи, например `validation.titleRequired`, `validation.contentRequired`, `validation.categoryRequired`.
- В `createRecipe` / `updateRecipe` при `!parsed.success` возвращать  
  `{ error: parsed.error.flatten().fieldErrors.title?.[0] ?? 'validation.error' }`  
  (или аналог с приоритетом полей). Важно, чтобы в `error` попадали только ключи.
- Остальные ответы: `{ error: 'errors.authRequired' }`, `'errors.notFound'`, `'errors.noPermission'` и т.д.

**`app/actions/categories.ts`:**

- Аналогично: валидация Zod и все `return { error: '...' }` перевести на ключи  
  (`validation.nameRequired`, `validation.nameTooLong`, `errors.categoryExists`, `errors.categoryLimit` и т.п.).

**Клиент:**  
В формах и диалогах при `res?.error` вызывать `alert(t(res.error))` (или показывать в UI). В `LikeButton` при ошибке от API — `setError(t('errors.tryLater'))`.

---

## Шаг 8. API-роут лайков

**`app/api/recipes/[id]/like/route.ts`:**

- При `500` возвращать, например, `{ error: 'errors.tryLater' }` вместо «Попробуйте позже».
- В `LikeButton` убрать проверку на точную строку; всегда показывать `t(data.error || 'errors.tryLater')` при `!res.ok` (и при `catch`).

---

## Шаг 9. Черногорские переводы

- Заполнить `messages/cnr.json` по ключам из `ru.json`.
- Черногорский — латиница (подобно сербскому латиницей). При необходимости можно свериться с стандартными формулировками в черногорской локализации.
- Особое внимание: склонения («рецепт» / «рецепта» / «рецептов») — в `cnr` подобрать эквиваленты.

---

## Шаг 10. Тестирование и безопасность

**Чек-лист:**

1. Переключение RU ↔ ME: смена флагов, обновление всех видимых подписей без перезагрузки (или с одной `router.refresh()`).
2. После перезагрузки страницы выбранный язык сохраняется (cookie).
3. Главная, дашборд, «Мои рецепты», «Публичные», «Категории», избранное, настройки и т.д. — всё переводится, layout не ломается.
4. Поиск, пагинация, вид (карточки/таблица), сортировка работают как раньше; меняется только текст кнопок/заголовков.
5. Формы: создание/редактирование рецепта, добавление категории. Ошибки валидации отображаются на выбранном языке.
6. Лайки, избранное, удаление рецепта, скачивание — без регрессий; сообщения об ошибках переведены.
7. Мобильная вёрстка: сайдбар, хедер, переключатель языков отображаются корректно.

**Риски и откат:**

- Все изменения локализованы в i18n, провайдере, компонентах и экшенах. Можно откатить правки по этому плану (удалить `LocaleProvider`, `LangSwitcher`, вернуть жёстко заданные строки, убрать cookie) и вернуться к текущему состоянию.
- Не трогаем БД, миграции, Prisma, Auth, роутинг, `searchParams`.

---

## Порядок внедрения (пошагово)

1. **Инфра:** `lib/i18n.ts`, `messages/ru.json` и `messages/cnr.json` с минимальным набором ключей (главная, хедер, футер).
2. **Провайдер и переключатель:** `LocaleProvider`, `LangSwitcher`, интеграция в layout и `Header`.
3. **Header / Footer:** переход на `t(...)`, плейсхолдер «Страница в разработке».
4. **Главная страница:** все строки через `t(...)`.
5. **Dashboard layout и общие блоки:** заголовки, пагинация.
6. **Страницы дашборда:** «Мои рецепты», «Публичные», «Категории» и т.д.
7. **Клиентские компоненты:** сайдбар, поиск, ViewToggle, SortSwitcher, диалоги, карточки, таблица, лайки, футер-плейсхолдеры.
8. **Server Actions и API:** переход на ключи ошибок, отображение через `t(...)` на клиенте.
9. **Полная инвентаризация строк** и дополнение `ru`/`cnr`, затем финальное тестирование.

После каждого шага — быстрая проверка в браузере и что ничего не сломалось (логин, рецепты, поиск, пагинация).

---

## Кратко по архитектуре

| Элемент            | Где хранится / как используется                          |
|--------------------|----------------------------------------------------------|
| Выбор языка        | Cookie `NEXT_LOCALE` (`ru` \| `cnr`)                     |
| Строки             | `messages/ru.json`, `messages/cnr.json`                  |
| Сервер             | `getLocale()` из cookie, `getT(locale)`                  |
| Клиент             | `LocaleProvider` + `useTranslations()` / `useLocale()`   |
| Переключатель      | Кнопки-флаги 🇷🇺 / 🇲🇪 в `Header`                        |
| URL и searchParams | Без изменений                                            |

Так можно добавить черногорский и переключатель языков, не нарушая функционал приложения.
