# Шаг 10. Тестирование и безопасность i18n

## Чек-лист

### 1. Переключение RU ↔ ME
- [ ] Кнопки-флаги 🇷🇺 / 🇲🇪 в шапке отображаются.
- [ ] Клик по флагу меняет язык; видимые подписи обновляются (при необходимости — после `router.refresh()`).
- [ ] Активный язык визуально выделен (бордер/фон).

### 2. Сохранение выбора языка
- [ ] Выбрать ME (🇲🇪), перезагрузить страницу — интерфейс остаётся на черногорском.
- [ ] Выбрать RU (🇷🇺), перезагрузить — интерфейс на русском.
- [ ] Cookie `NEXT_LOCALE`: значения `ru` | `cnr`, путь `/`, срок хранения достаточный (например, 1 год).

### 3. Перевод страниц и layout
- [ ] **Главная** `/`: hero, «Недавние»/«Популярные», кнопка входа, пустые состояния.
- [ ] **Дашборд** `/dashboard`: «Личный кабинет», «Мои рецепты», «Новый рецепт», пустое состояние, пагинация.
- [ ] **Публичные** `/dashboard/public`: заголовки, пустое состояние, пагинация, сортировка, вид.
- [ ] **Категории** `/dashboard/categories`: заголовки, «X из 20 категорий», склонения рецепт/рецепта/рецептов, пустое состояние.
- [ ] **Избранное** `/dashboard/favorites`, **История** `/dashboard/history`, **Профиль** `/dashboard/profile`, **Настройки** `/dashboard/settings` — заголовки и общий layout не ломаются.
- [ ] **Header**: Вход/Выход, переключатель языков.
- [ ] **Footer**: Контакты, О нас, «Страница в разработке» (по клику).
- [ ] **Сайдбар**: все пункты меню, «Меню», «Открыть/Закрыть меню», «Пользователь».

### 4. Поиск, пагинация, вид, сортировка
- [ ] Поиск по названию работает; плейсхолдер переведён.
- [ ] Пагинация «Назад» / «Вперёд» / «N из M» переведена, переход по страницам работает.
- [ ] Переключатель «Карточки» / «Таблица» переведён, переключение работает.
- [ ] Сортировка «Популярные» / «Новые» (на публичной) переведена, работает.
- [ ] URL и `searchParams` (`?q=`, `?page=`, `?view=`, `?sort=`) без изменений.

### 5. Формы и валидация
- [ ] **Создание рецепта**: диалог «Новый рецепт», поля, кнопки. Оставить пустые поля → отправить: ошибки валидации на выбранном языке.
- [ ] **Редактирование рецепта**: то же для «Редактировать рецепт»; ошибки валидации переведены.
- [ ] **Добавление категории**: диалог «Добавить категорию», валидация (пустое имя, лимит, дубликат) — сообщения на выбранном языке.

### 6. Лайки, избранное, удаление, скачивание
- [ ] Лайки: поставить/снять на публичном рецепте; при ошибке API сообщение переведено (`errors.tryLater` и др.).
- [ ] Избранное: добавить/убрать — без регрессий.
- [ ] Удаление рецепта: подтверждение «Удалить рецепт?» переведено; удаление работает.
- [ ] Скачивание рецепта: файл формируется; при использовании `t(...)` заголовки «Категория», «Обновлено», «Статус», «Публичный»/«Приватный» в файле на выбранном языке.

### 7. Мобильная вёрстка
- [ ] Сайдбар: кнопка «Меню», выдвижная панель, «Закрыть меню».
- [ ] Хедер: логотип, переключатель языков, Вход/Выход — всё читаемо и кликабельно.
- [ ] Переключатель языков (флаги) не обрезается и не ломает layout на узких экранах.

---

## Быстрая прогонка (ручная)

1. Запустить `npm run dev`.
2. Открыть `/`, переключить RU → ME → RU, убедиться в смене текста.
3. Перезагрузить страницу — язык сохраняется.
4. Войти в аккаунт, пройти: `/dashboard`, `/dashboard/public`, `/dashboard/categories`.
5. Создать рецепт (успех), затем отправить форму с пустым названием — проверить перевод ошибки.
6. Добавить категорию, затем попытаться добавить с пустым именем — проверить перевод.
7. На публичной: лайк, избранное, скачать рецепт, удалить (если владелец).

---

## Автоматические проверки

- **TypeScript**: `npx tsc --noEmit`
- **Линт**: `npm run lint`
- **Ключи i18n**: `npm run check:i18n` — все ключи из `ru.json` есть в `cnr.json`, оба файла валидный JSON.

Перед коммитом или в CI можно запускать: `npx tsc --noEmit && npm run lint && npm run check:i18n`.

---

## Риски и откат

- Изменения ограничены i18n, `LocaleProvider`, `LangSwitcher`, компонентами и экшенами. Для отката:
  - Удалить `LocaleProvider`, `LangSwitcher`, правки в `layout`/`Header`/`Footer`.
  - Вернуть жёстко заданные строки в страницах и компонентах.
  - Убрать использование cookie `NEXT_LOCALE` и логику `getLocale`/`getT`/`getMessages`.
- **Не менялись**: БД, миграции, Prisma, Auth, роутинг, `searchParams`.

---

## Безопасность

- **Cookie `NEXT_LOCALE`**: хранит только предпочтение языка (`ru` | `cnr`), не содержит персональных или чувствительных данных.
- Установка cookie с клиента (`document.cookie`) — допустимо для локали; при необходимости можно перенести на API-роут с `Set-Cookie`.
- Авторизация, сессии, права доступа не зависят от локали. Язык не влияет на проверки в Server Actions и API.
