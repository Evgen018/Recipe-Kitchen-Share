# Recipe Kitchen Share

Минимальный рабочий проект на Next.js (App Router) + Prisma + NeonDB (PostgreSQL), готовый к деплою на Vercel.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```powershell
npm install
```

### 2. Настройка базы данных

1. Создайте проект в [Neon Dashboard](https://console.neon.tech)
2. Скопируйте строку подключения (Connection String)
3. Создайте файл `.env` в корне проекта:

```powershell
# Скопируйте пример
Copy-Item .env.example .env
```

4. Откройте `.env` и вставьте вашу строку подключения:

```
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"
```

### 3. Настройка Prisma

```powershell
# Применить схему к базе данных
npm run db:push

# Или создать миграцию (рекомендуется для продакшена)
npm run db:migrate
```

### 4. Заполнение базы данных (seed)

```powershell
npm run db:seed
```

### 5. Запуск проекта

```powershell
# Режим разработки
npm run dev

# Откройте http://localhost:3000
```

## 📦 Структура проекта

```
├── app/
│   ├── layout.tsx      # Корневой layout
│   ├── page.tsx        # Главная страница с чтением данных из БД
│   └── globals.css     # Глобальные стили
├── prisma/
│   ├── schema.prisma   # Схема Prisma с моделью Note
│   └── seed.ts         # Seed файл для начальных данных
├── .env.example        # Пример переменных окружения
├── package.json        # Зависимости и скрипты
└── vercel.json         # Конфигурация для Vercel
```

## 🗄️ Модель данных

### Note
- `id` (uuid) - уникальный идентификатор
- `title` (string) - заголовок заметки
- `createdAt` (DateTime) - дата создания

## 🚢 Деплой на Vercel

### 1. Подготовка

1. Убедитесь, что все изменения закоммичены в Git
2. Создайте репозиторий на GitHub (если еще нет)

### 2. Настройка Vercel

1. Перейдите на [Vercel](https://vercel.com)
2. Импортируйте ваш GitHub репозиторий
3. В настройках проекта добавьте переменную окружения:
   - **Name**: `DATABASE_URL`
   - **Value**: ваша строка подключения из Neon

### 3. Деплой

Vercel автоматически:
- Установит зависимости (`npm install`)
- Сгенерирует Prisma Client (`prisma generate`)
- Соберет проект (`next build`)
- Задеплоит приложение

### 4. Применение миграций на Vercel

После первого деплоя выполните миграции:

```powershell
# Локально (миграции применятся к продакшен БД)
npx prisma migrate deploy
```

Или используйте Neon Dashboard для выполнения SQL миграций.

### 5. Заполнение данных (опционально)

После деплоя можно запустить seed на продакшене (не рекомендуется для больших проектов):

```powershell
# Установите DATABASE_URL для продакшена
$env:DATABASE_URL="your-production-connection-string"
npm run db:seed
```

## 📝 Доступные команды

```powershell
# Разработка
npm run dev              # Запуск dev сервера

# База данных
npm run db:push          # Применить схему без миграции
npm run db:migrate       # Создать и применить миграцию
npm run db:seed          # Заполнить базу тестовыми данными
npm run db:studio        # Открыть Prisma Studio

# Сборка
npm run build            # Сборка для продакшена
npm start                # Запуск продакшен сервера
```

## 🔧 Технологии

- **Next.js 14** - React фреймворк с App Router
- **TypeScript** - типизация
- **Prisma** - ORM для работы с БД
- **NeonDB** - бессерверный PostgreSQL
- **Vercel** - платформа для деплоя

## 📄 Лицензия

См. файл [LICENSE](LICENSE)
