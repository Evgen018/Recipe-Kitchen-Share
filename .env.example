# База данных (NeonDB, PostgreSQL)
DATABASE_URL="postgresql://user:password@host/database?sslmode=verify-full"

# Auth.js / Google OAuth
GOOGLE_CLIENT_ID="ваш-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="ваш-google-client-secret"
AUTH_SECRET="сгенерируйте-через-openssl-rand-base64-32"

# Опционально для продакшена (Vercel): явный URL приложения для OAuth callbacks.
# Если не задан, Auth.js использует host из запроса (при trustHost: true).
# AUTH_URL="https://ваш-проект.vercel.app"
